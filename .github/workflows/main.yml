on:
  workflow_dispatch:

jobs:
  compilarSpring:
    name: compilar spring
    runs-on: ubuntu-22.04
    steps:
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'corretto'
